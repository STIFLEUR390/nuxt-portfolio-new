# Tests

Aucune suite de tests configurée.

Pas de CI/CD workflows.

La vérification se fait via :
- `pnpm lint` (ESLint)
- `pnpm typecheck` (vue-tsc)

Ces deux commandes sont le seul filet de sécurité.
